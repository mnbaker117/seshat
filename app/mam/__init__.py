"""
MAM-specific code: IRC client, announce parser, .torrent grabber,
session cookie management.

Every module under this package assumes MyAnonamouse as the upstream.
There are no tracker abstractions and there will be none — the codebase
stays small precisely because we're MAM-only by design.
"""
