# Production runtime dependencies for Seshat.
# Test deps live in requirements-dev.txt.

# Web framework
fastapi==0.121.0
uvicorn[standard]==0.30.0

# Server-Sent Events (live torrent progress, MAM stats, toasts)
sse-starlette==3.3.4

# Async DB
aiosqlite==0.20.0

# HTTP (MAM cookie validation, .torrent grab, qBittorrent API)
httpx==0.27.0

# Background scheduling (Phase 3: cookie checks, weekly audit, daily digest)
apscheduler==3.10.4

# Data validation (FastAPI request/response models)
pydantic==2.8.0

# Auth scaffolding (Phase 3 — versions matched to AthenaScout)
bcrypt==4.2.1
itsdangerous==2.2.0

# Audiobook metadata reading (M4B/MP3 tags via mutagen)
mutagen==1.47.0

# Cover image perceptual hashing for MAM URL verification (Part C).
# imagehash provides pHash (DCT-based); Pillow is its image backend.
# Pillow is also transitively pulled by cloudscraper/lxml today, but
# pinning here makes the runtime contract explicit. ~3MB on the slim
# image. Validated 2026-05-09 against 16 image pairs (right-Possibles
# cluster at distance 0-6, wrong-matches at 28-36).
Pillow==12.2.0
ImageHash==4.3.2

# Metadata scraper HTML parsing (Goodreads, Amazon, etc.).
# lxml 6.1.0 patches CVE-2026-41066 (default iterparse/ETCompatXMLParser
# config allowed XXE to local files). We only use `from lxml import html`
# in the Kobo scrapers — the XXE paths aren't exercised here — but a
# clean dep graph beats carrying a known-CVE lib.
beautifulsoup4==4.12.3
lxml==6.1.0

# Cloudflare bypass for Kobo scraping (interactive JS challenge solver)
cloudscraper==1.2.71

# v2.11.0 — TLS-fingerprint impersonation for sources fronted by
# Akamai Bot Manager (Amazon's IPv4 path) and (Phase 5.5) Cloudflare's
# silent-scoring layer (Goodreads). curl_cffi drives libcurl-impersonate
# which exactly replicates Chrome's JA3 fingerprint at the protocol
# layer — defeats the per-request bot-scoring that cloudscraper alone
# can't bypass (cloudscraper still uses Python's standard TLS).
# AmazonSource uses `AsyncSession(impersonate="chrome120")`; Phase 5.5
# Goodreads source-scan path will use the same lib + `impersonate`.
curl_cffi>=0.7.0

# Fernet symmetric encryption for the secret store.
# Patch cascade: 46.0.5 fixes CVE-2026-26007 (SECT-curve subgroup
# validation, HIGH); 46.0.6 rolls in CVE-2026-34073 (DNS name
# constraint enforcement) and a patched OpenSSL wheel (older
# CVE-2024-12797); 46.0.7 closes CVE-2026-39892 (buffer overflow
# when non-contiguous Python buffers are passed to Hash-family APIs
# — not a path we hit, but a clean dep graph is worth keeping).
# Fernet API is unchanged across the whole range — no code impact.
cryptography==46.0.7
