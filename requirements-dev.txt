# Test dependencies — NOT installed in the production Docker image.
#
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.0
pytest-asyncio==0.24.0
